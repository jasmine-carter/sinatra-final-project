<h2>Hey look at all of your games, <%=current_user.username%>!</h2>

<% if current_user.games == 0%>
<p>It looks like you don't have any games. Why don't you add one to your library?</p>
<a href="/games/new">Create a Game</a>
  <%else%>
  <% Game.all.each do |g| %>
    <% if g.user_id == current_user.id %>
      <li><%="Name: #{g.name}"%></li>
      <li><%="Rating: #{g.rating}"%></li>
      <li><%="Review: #{g.review}"%></li>
  <% end %>
  <% end %>
  <br>
  <a href="/games/new"> Add Another Game?</a>
  <% end %>
